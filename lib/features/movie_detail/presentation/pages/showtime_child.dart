import 'package:flutter/material.dart';

class ShowtimePage extends StatelessWidget {
  const ShowtimePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Showtime'));
  }
}
